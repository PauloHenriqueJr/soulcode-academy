import { PacienteDirective } from './paciente.directive';

describe('PacienteDirective', () => {
  it('should create an instance', () => {
    const directive = new PacienteDirective();
    expect(directive).toBeTruthy();
  });
});
