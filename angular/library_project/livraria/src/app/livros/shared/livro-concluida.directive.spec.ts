import { LivroConcluidaDirective } from './livro-concluida.directive';

describe('LivroConcluidaDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new LivroConcluidaDirective();
  //   expect(directive).toBeTruthy();
  //});
});
